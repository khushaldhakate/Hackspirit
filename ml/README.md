# Machine Learning Module - PhishGuard

This directory is reserved for the pretrained malicious/phishing URL classification model and feature extraction pipelines.

## Structure
- `models/` - Storage for pretrained model artifacts (e.g., weights, ONNX, joblib).
- `scripts/` - Inference and feature extraction scripts.

*Note: ML integration will be added in subsequent phases.*
